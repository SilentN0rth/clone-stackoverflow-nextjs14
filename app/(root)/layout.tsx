import "../globals.css";
import React, { ReactNode } from "react";
const Layout = ({ children }: { children: ReactNode }) => {
  return <main className="flex-center background-light850_dark100 relative">DevOverflow</main>;
};

export default Layout;
