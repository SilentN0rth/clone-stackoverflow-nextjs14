const RightSidebar = () => {
    return (
        <aside className="background-light900_dark200 global-padding light-border custom-scrollbar fixed inset-y-0 right-0 flex max-h-screen flex-col justify-between overflow-y-auto border-r pt-36 max-sm:hidden">
            RightSidebar
        </aside>
    );
};

export default RightSidebar;
